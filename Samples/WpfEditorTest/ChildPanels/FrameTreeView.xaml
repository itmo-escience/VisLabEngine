<UserControl x:Class="WpfEditorTest.ChildPanels.FrameTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfEditorTest.ChildPanels"
             mc:Ignorable="d" 
             d:DesignHeight="264" d:DesignWidth="264">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Width="auto" Height="32" VerticalAlignment="Top" MouseLeftButtonDown="Border_MouseDown" Grid.Row="0">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="LightGray" Offset="1"/>
                    <GradientStop Color="White" Offset="0.5"/>
                    <GradientStop Color="Gray"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="17"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="HierarcyView" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="14" Grid.Column="0" Margin="4,6,0,6" Height="20" Width="80"></TextBlock>
                <Button Name="SaveScene" Grid.Column="1" Background="White" Margin="4,4,4,4" Click="SaveScene_Click">
                    <TextBlock Text="SaveScene" HorizontalAlignment="Center"></TextBlock>
                </Button>
                <Button Name="LoadScene" Grid.Column="2" Background="White" Margin="4,4,4,4" Click="LoadScene_Click">
                    <TextBlock Text="LoadScene" HorizontalAlignment="Center"></TextBlock>
                </Button>
            </Grid>
        </Border>
        <ScrollViewer Height="auto" Width="auto" Background="LightGray" Grid.Row="1">
            <TreeView Name="ElementHierarcyView" Background="LightGray" Height="auto" Width="auto">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                        <TextBlock Text="{Binding Text}" Tag="{Binding}" MouseDown="TextBlock_MouseDown"/>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </ScrollViewer>
    </Grid>
</UserControl>
