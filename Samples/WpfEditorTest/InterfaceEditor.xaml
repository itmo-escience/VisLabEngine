<Window x:Class="WpfEditorTest.InterfaceEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfEditorTest"
        xmlns:ZWpfLib="clr-namespace:ZWpfLib;assembly=ZWpfLib"
        xmlns:system ="clr-namespace:System;assembly=mscorlib"
        xmlns:math ="clr-namespace:Fusion.Core.Mathematics;assembly=Fusion"
        xmlns:panels ="clr-namespace:WpfEditorTest.ChildPanels"
        mc:Ignorable="d"
        Title="InterfaceEditor" Height="675" Width="1200" WindowStartupLocation="CenterScreen" KeyDown="Window_KeyDown">
    <Window.Resources>
        <local:PropertyValueConverter x:Key="propValueConverter">
        </local:PropertyValueConverter>
    </Window.Resources>
    <Grid Width="auto" Height="auto" Name="LocalGrid" MouseMove="LocalGrid_MouseMove" MouseLeftButtonUp="LocalGrid_MouseLeftButtonUp" >
        <ZWpfLib:DXElement HorizontalAlignment="Left" Height="auto" Width="auto" x:Name="DxElem" VerticalAlignment="Top" Grid.Column="0" Grid.RowSpan="3" ScrollViewer.CanContentScroll="True" MouseDown="DxElem_MouseDown" />
        <!--123-->
        <!--<panels:FrameDetails Width="264" Height="264">
            
        </panels:FrameDetails>-->
       <!--123-->
        <!--<ScrollViewer Height="auto" Width="264" Grid.Column="1" Grid.Row="2">
            <TreeView Name="ElementHierarcyView" Height="auto" Width="auto">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                        <TextBlock Text="{Binding Text}" Tag="{Binding}" MouseDown="TextBlock_MouseDown"/>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </ScrollViewer>-->

        <!--<ScrollViewer Height="auto" Width="264" Grid.Column="1" Grid.Row="0">
            <StackPanel Height="auto" Width="auto">
                <ItemsControl Name="AvailableFrames">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" BorderBrush="Gray" Background="SkyBlue" MinHeight="32" Width="auto">
                                <TextBlock Text="{Binding Path=Name}"
                                               TextAlignment ="Center"
				                               TextWrapping = "Wrap"
				                               VerticalAlignment = "Center"
                                               FontFamily="Times New Roman">
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>-->
        
        <!--<ScrollViewer Height="auto" Width="264" Grid.Column="1" Grid.Row="1">
                <StackPanel Height="auto" Width="auto">
                    <ItemsControl Name="FrameDetails">
                        --><!--<ItemsControl.Resources>
                            <DataTemplate DataType="{x:Type system:Int32}">
                                <Rectangle Width="50" Height="50" Fill="Red" />
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type math:Color}">
                                <Rectangle Width="50" Height="50" Fill="Blue" />
                            </DataTemplate>
                        </ItemsControl.Resources>--><!--
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Height="auto" Width="auto" Orientation="Horizontal" >
                                    <Border BorderThickness="1" BorderBrush="Gray" Background="Red" MinHeight="32" MinWidth="100">
                                        <TextBlock TextAlignment ="Left"
				                               TextWrapping = "Wrap"
				                               VerticalAlignment = "Center"
                                               FontFamily="Times New Roman"
                                         Text="{Binding Path=PropName}">
                                        </TextBlock>
                                    </Border>
                                    <Border BorderThickness="1" BorderBrush="Gray" Background="Green" MinHeight="32" Width="auto" HorizontalAlignment="Stretch">
                                        <TextBox TextAlignment ="Center"
				                               TextWrapping = "Wrap"
				                               VerticalAlignment = "Center"
                                               FontFamily="Times New Roman"
                                         Text="{Binding Path=Prop, Converter={StaticResource propValueConverter}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                            --><!--<TextBox.Text>
                                        <MultiBinding Converter="{StaticResource propValueConverter}">
                                            <Binding Path="."></Binding>
                                        </MultiBinding>
                                    </TextBox.Text>--><!--
                                        </TextBox>
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>-->
    </Grid>
</Window>
